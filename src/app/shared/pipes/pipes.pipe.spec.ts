/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PipesPipe } from './pipes.pipe';

describe('Pipe: Pipese', () => {
  it('create an instance', () => {
    let pipe = new PipesPipe();
    expect(pipe).toBeTruthy();
  });
});
